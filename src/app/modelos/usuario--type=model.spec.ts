import { UsuarioType=model } from './usuario--type=model';

describe('UsuarioType=model', () => {
  it('should create an instance', () => {
    expect(new UsuarioType=model()).toBeTruthy();
  });
});
